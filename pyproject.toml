[project]
name = "xtr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = ["packages/xtr-*"]

[build-system]
requires = ["uv_build>=0.9.18,<0.13.0"]
build-backend = "uv_build"