# .env.test — committed, read when APP_ENV=test. .env.local is NOT read in test (test_envs),
# so every machine runs the tests with the same values; .env.test.local still is.
SHOP_NAME="The Bookshop (test)"
SHOP_MEMBER_DISCOUNT=0
SHOP_LOG_LEVEL=warning
APP_TIMEZONE=UTC
